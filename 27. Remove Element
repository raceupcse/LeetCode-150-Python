def removeElement(self, nums: List[int], val: int) -> int:
        res = []
        for n in nums:
            if n!=val:
                res.append(n)
        print(res)
        for i in range(len(res)):
            nums[i]=res[i]
        return len(res)
